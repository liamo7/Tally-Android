<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:id="@+id/container"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="?android:selectableItemBackground"
                android:padding="@dimen/chat_message_container_padding">

    <TextView
        android:id="@+id/message"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/chat_message_margin_x"
        android:layout_toStartOf="@id/time"
        android:textColor="@color/colorPrimaryText"
        android:textSize="@dimen/chat_message_text_size"
        tools:text="This is an example of a chat message that would be shown within the chat for a specific module."/>

    <TextView
        android:id="@+id/time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:textColor="@color/colorSecondaryText"
        tools:text="@string/pretty_time_one_hour"/>

</RelativeLayout>